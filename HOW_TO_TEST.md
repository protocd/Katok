# Как тестировать проект (пошаговая инструкция)

## Шаг 1: Запустить локальный сервер

### Если используете XAMPP (Windows):
1. Откройте **XAMPP Control Panel**
2. Нажмите **Start** для **Apache**
3. Нажмите **Start** для **MySQL**
4. Оба должны быть зелеными ✅

### Если используете MAMP (Mac):
1. Запустите MAMP
2. Нажмите **Start Servers**

---

## Шаг 2: Создать базу данных

### Способ 1: Автоматически (проще)

1. Откройте в браузере:
   ```
   http://localhost/rinks-moscow-app/backend/test/setup_database.php
   ```

2. Должно появиться:
   ```
   ✅ Подключение к MySQL успешно
   ✅ База данных 'rinks_moscow' создана
   ✅ Таблица 'users' создана
   ✅ Таблица 'rinks' создана
   ...
   ```

### Способ 2: Через phpMyAdmin

1. Откройте http://localhost/phpmyadmin
2. Создайте базу данных `rinks_moscow`
3. Импортируйте файл `sql/database_final.sql`

---

## Шаг 3: Настроить config.php

1. Скопируйте файл:
   - `backend/config/config.example.php` 
   - → `backend/config/config.php`

2. Откройте `backend/config/config.php` и проверьте настройки:
   ```php
   define('DB_HOST', 'localhost');
   define('DB_NAME', 'rinks_moscow');
   define('DB_USER', 'root');        // Обычно root для XAMPP
   define('DB_PASS', '');            // Обычно пустой для XAMPP
   ```

---

## Шаг 4: Тестировать подключение к БД

Откройте в браузере:
```
http://localhost/rinks-moscow-app/backend/test/test_connection.php
```

**Должно показать:**
```
✅ Подключение к базе данных успешно!
✅ Таблица 'users' существует (записей: 0)
✅ Таблица 'rinks' существует (записей: 0)
...
```

**Если ошибка:**
- Проверьте, запущен ли MySQL
- Проверьте настройки в config.php
- Убедитесь, что база данных создана

---

## Шаг 5: Тестировать классы

Откройте в браузере:
```
http://localhost/rinks-moscow-app/backend/test/test_classes.php
```

**Должно показать:**
```
✅ Хеширование паролей работает
✅ Получено районов: 0
✅ Класс Visit создан
```

---

## Шаг 6: Тестировать API (через браузер)

### Тест 1: Получить список катков
Откройте в браузере:
```
http://localhost/rinks-moscow-app/backend/api/rinks.php
```

**Должно показать:**
```json
{
  "success": true,
  "data": {
    "results": [],
    "count": 0,
    "total": 0
  }
}
```

### Тест 2: Регистрация пользователя

**Через браузер (не сработает для POST):**
Нужно использовать Postman или curl

**Через curl (в командной строке):**
```bash
curl -X POST http://localhost/rinks-moscow-app/backend/api/auth/register.php ^
  -H "Content-Type: application/json" ^
  -d "{\"email\":\"test@test.com\",\"password\":\"test123\",\"name\":\"Тест\"}"
```

---

## Шаг 7: Тестировать через Postman (рекомендуется)

### Установка Postman:
1. Скачайте с https://www.postman.com/downloads/
2. Установите
3. Запустите

### Тест регистрации:

1. Создайте новый запрос (New → HTTP Request)
2. Метод: **POST**
3. URL: `http://localhost/rinks-moscow-app/backend/api/auth/register.php`
4. Вкладка **Headers:**
   - Key: `Content-Type`
   - Value: `application/json`
5. Вкладка **Body:**
   - Выберите **raw**
   - Выберите **JSON**
   - Введите:
   ```json
   {
     "email": "test@test.com",
     "password": "test123",
     "name": "Тест Тестов"
   }
   ```
6. Нажмите **Send**

**Ожидаемый ответ:**
```json
{
  "success": true,
  "data": {
    "user": {
      "id": 1,
      "email": "test@test.com",
      "name": "Тест Тестов"
    }
  },
  "message": "Регистрация прошла успешно"
}
```

### Тест авторизации:

1. Метод: **POST**
2. URL: `http://localhost/rinks-moscow-app/backend/api/auth/login.php`
3. Body (JSON):
   ```json
   {
     "email": "test@test.com",
     "password": "test123"
   }
   ```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "data": {
    "user": {
      "id": 1,
      "email": "test@test.com",
      "name": "Тест Тестов"
    }
  }
}
```

---

## Шаг 8: Добавить тестовые данные

### Добавить катки (через phpMyAdmin):

1. Откройте http://localhost/phpmyadmin
2. Выберите базу `rinks_moscow`
3. Таблица `rinks` → Вставить
4. Добавьте несколько катков вручную или импортируйте из CSV

**Пример данных для одного катка:**
```sql
INSERT INTO rinks (name, address, district, latitude, longitude, is_paid) 
VALUES ('Каток в Парке Горького', 'Крымский Вал, 9', 'ЦАО', 55.7314, 37.6031, false);
```

---

## Чеклист тестирования:

- [ ] MySQL запущен
- [ ] Apache запущен
- [ ] База данных создана
- [ ] config.php настроен
- [ ] test_connection.php работает
- [ ] test_classes.php работает
- [ ] API rinks.php возвращает JSON
- [ ] Регистрация работает (через Postman)
- [ ] Авторизация работает (через Postman)
- [ ] Тестовые данные добавлены

---

## Частые проблемы:

### Ошибка "Could not find driver":
- Установите расширение `php_pdo_mysql` в php.ini
- Перезапустите Apache

### Ошибка подключения к БД:
- Проверьте, запущен ли MySQL
- Проверьте логин/пароль в config.php

### Ошибка 404:
- Проверьте путь к файлам
- Убедитесь, что Apache запущен

### CORS ошибка:
- Это нормально при тестировании через браузер
- Используйте Postman для тестирования API

---

## Готово!

Если все тесты прошли - можно переходить к разработке frontend! 🎉
