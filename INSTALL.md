# Инструкция по установке и запуску

## Шаг 1: Установка локального сервера

### Windows:
1. Скачайте и установите **XAMPP** с https://www.apachefriends.org/
2. Запустите XAMPP Control Panel
3. Запустите **Apache** и **MySQL**

### Mac:
1. Скачайте и установите **MAMP** с https://www.mamp.info/
2. Запустите MAMP
3. Нажмите "Start Servers"

### Linux:
```bash
sudo apt-get install apache2 mysql-server php php-mysql
sudo systemctl start apache2
sudo systemctl start mysql
```

---

## Шаг 2: Настройка базы данных

### Вариант А: Автоматическая настройка (проще)

1. Откройте файл `backend/test/setup_database.php` в браузере:
   ```
   http://localhost/rinks-moscow-app/backend/test/setup_database.php
   ```

2. Скрипт создаст базу данных и все таблицы автоматически

### Вариант Б: Ручная настройка

1. Откройте phpMyAdmin: http://localhost/phpmyadmin

2. Создайте базу данных:
   - Имя: `rinks_moscow`
   - Кодировка: `utf8mb4_unicode_ci`

3. Импортируйте SQL скрипт:
   - Выберите базу данных `rinks_moscow`
   - Вкладка "Импорт"
   - Выберите файл `sql/database_final.sql`
   - Нажмите "Вперед"

---

## Шаг 3: Настройка конфигурации

1. Скопируйте файл конфигурации:
   ```
   backend/config/config.example.php → backend/config/config.php
   ```

2. Откройте `backend/config/config.php` и заполните:
   ```php
   define('DB_HOST', 'localhost');
   define('DB_NAME', 'rinks_moscow');
   define('DB_USER', 'root');        // Обычно root для XAMPP
   define('DB_PASS', '');            // Обычно пустой для XAMPP
   ```

---

## Шаг 4: Проверка работы

### Тест 1: Подключение к БД
Откройте в браузере:
```
http://localhost/rinks-moscow-app/backend/test/test_connection.php
```

Должно показать: ✅ Подключение успешно

### Тест 2: Работа классов
Откройте в браузере:
```
http://localhost/rinks-moscow-app/backend/test/test_classes.php
```

### Тест 3: API эндпоинты
Используйте Postman или curl:

```bash
# Получить список катков
curl http://localhost/rinks-moscow-app/backend/api/rinks.php
```

---

## Шаг 5: Получение данных о катках

1. Скачайте датасет "Катки Москвы" с портала открытых данных:
   https://data.mos.ru/

2. Импортируйте данные в таблицу `rinks` через phpMyAdmin или создайте PHP скрипт для импорта

---

## Структура URL для доступа:

- **Frontend:** `http://localhost/rinks-moscow-app/frontend/`
- **Backend API:** `http://localhost/rinks-moscow-app/backend/api/`
- **Тесты:** `http://localhost/rinks-moscow-app/backend/test/`

---

## Возможные проблемы:

### Ошибка подключения к БД:
- Проверьте, запущен ли MySQL
- Проверьте настройки в config.php
- Проверьте права пользователя MySQL

### Ошибка 404:
- Убедитесь, что файлы находятся в правильной папке
- Проверьте настройки Apache (mod_rewrite)

### Ошибка CORS:
- Проверьте настройки в `backend/includes/cors.php`
- Добавьте свой домен в ALLOWED_ORIGINS

---

## Готово!

Если все тесты прошли успешно - можно переходить к разработке frontend! 🎉
