# ⚙️ НАСТРОЙКА КОНФИГУРАЦИИ

## ✅ ЧТО УЖЕ НАСТРОЕНО:

### 1. Backend конфигурация (`backend/config/config.php`)
- ✅ База данных: `localhost`, `rinks_moscow`, `root`, пароль пустой
- ✅ Порт: 8080 (для XAMPP)
- ✅ CORS: разрешены localhost и 127.0.0.1 на портах 80 и 8080
- ✅ Максимальное расстояние для чек-ина: 1000м (1км)
- ✅ Режим отладки: включен

### 2. Frontend конфигурация (`frontend/js/api.js`)
- ✅ API URL: `http://localhost:8080/rinks-moscow-app/backend/api`
- ✅ Правильный порт: 8080

---

## 🔧 ЧТО НУЖНО ПРОВЕРИТЬ:

### 1. База данных MySQL

**Проверь, что:**
- MySQL запущен в XAMPP
- База данных `rinks_moscow` создана
- Пользователь `root` имеет доступ (пароль пустой)

**Если БД не создана:**
1. Открой phpMyAdmin: `http://localhost/phpmyadmin`
2. Создай базу данных `rinks_moscow`
3. Импортируй `sql/database_final.sql`

**Или через скрипт:**
```
http://localhost:8080/rinks-moscow-app/backend/test/create_all_tables.php
```

### 2. Пути в config.php

**Текущие настройки:**
```php
DB_HOST = 'localhost'
DB_NAME = 'rinks_moscow'
DB_USER = 'root'
DB_PASS = ''  // Если у тебя есть пароль для root, укажи его здесь
```

**Если у тебя другой пароль для MySQL:**
Отредактируй `backend/config/config.php`:
```php
define('DB_PASS', 'твой_пароль');
```

### 3. Пути в api.js

**Текущая настройка:**
```javascript
baseUrl: 'http://localhost:8080/rinks-moscow-app/backend/api'
```

**Если твой проект в другом месте:**
Отредактируй `frontend/js/api.js`:
```javascript
baseUrl: 'http://localhost:8080/путь/к/проекту/backend/api'
```

### 4. Yandex Maps API ключ

**Если нужна карта:**
1. Получи ключ на https://developer.tech.yandex.ru/
2. Добавь в `frontend/index.html` и `frontend/rink.html`:
```html
<script src="https://api-maps.yandex.ru/2.1/?apikey=ТВОЙ_КЛЮЧ&lang=ru_RU"></script>
```

**Или используй без ключа (с ограничениями):**
```html
<script src="https://api-maps.yandex.ru/2.1/?lang=ru_RU"></script>
```

---

## 🧪 ПРОВЕРКА НАСТРОЙКИ:

### 1. Проверь подключение к БД:
```
http://localhost:8080/rinks-moscow-app/backend/test/test_connection.php
```

**Должно показать:** "Подключение успешно!"

### 2. Проверь API:
```
http://localhost:8080/rinks-moscow-app/backend/api/rinks.php
```

**Должно вернуть:** JSON с катками (может быть пустым, если БД пустая)

### 3. Проверь синхронизацию данных:
```
http://localhost:8080/rinks-moscow-app/backend/api/sync_data.php
```

**Должно:** загрузить катки из data.mos.ru (может занять время)

### 4. Открой главную страницу:
```
http://localhost:8080/rinks-moscow-app/frontend/index.html
```

**Должно:** открыться страница с поиском и картой

---

## ⚠️ ЧАСТЫЕ ПРОБЛЕМЫ:

### Проблема: "Ошибка подключения к базе данных"
**Решение:**
- Проверь, что MySQL запущен в XAMPP
- Проверь пароль в `config.php`
- Проверь, что база данных `rinks_moscow` существует

### Проблема: "CORS error" в консоли браузера
**Решение:**
- Проверь, что в `config.php` указан правильный порт (8080)
- Проверь, что `ALLOWED_ORIGINS` содержит твой URL

### Проблема: "404 Not Found" для API
**Решение:**
- Проверь путь в `api.js`: `baseUrl`
- Проверь, что файлы API находятся в `backend/api/`

### Проблема: Карта не загружается
**Решение:**
- Проверь, что Yandex Maps API подключен в HTML
- Проверь консоль браузера на ошибки
- Если нужен ключ - получи его на developer.tech.yandex.ru

---

## ✅ ВСЁ ГОТОВО!

Если все проверки прошли успешно, проект настроен и готов к работе!

**Следующий шаг:** Запусти синхронизацию данных и протестируй функционал.
