# 🌐 КАК ОТКРЫТЬ САЙТ

## Способ 1: Через XAMPP (рекомендуется)

### Шаг 1: Запустить XAMPP
1. Откройте **XAMPP Control Panel**
2. Нажмите **Start** для **Apache** (должен стать зеленым ✅)
3. Нажмите **Start** для **MySQL** (должен стать зеленым ✅)

### Шаг 2: Скопировать проект в htdocs
1. Откройте папку XAMPP (обычно `C:\xampp\htdocs`)
2. Скопируйте туда папку `rinks-moscow-app` из `C:\Users\kauiw\rinks-moscow-app`
3. Или создайте символическую ссылку

### Шаг 3: Открыть в браузере
Откройте в браузере:
```
http://localhost/rinks-moscow-app/frontend/index.html
```

---

## Способ 2: Если проект уже в htdocs

Если проект уже находится в `C:\xampp\htdocs\rinks-moscow-app`, просто откройте:
```
http://localhost/rinks-moscow-app/frontend/index.html
```

---

## Способ 3: Через встроенный PHP сервер (без XAMPP)

1. Откройте командную строку (PowerShell)
2. Перейдите в папку проекта:
   ```powershell
   cd C:\Users\kauiw\rinks-moscow-app
   ```
3. Запустите PHP сервер:
   ```powershell
   php -S localhost:8000
   ```
4. Откройте в браузере:
   ```
   http://localhost:8000/frontend/index.html
   ```

---

## Способ 4: Прямое открытие файла (ограниченно)

⚠️ **Внимание:** Этот способ не будет работать с API запросами из-за CORS!

1. Просто откройте файл:
   ```
   C:\Users\kauiw\rinks-moscow-app\frontend\index.html
   ```
   (двойной клик по файлу)

2. Но API не будет работать! Нужен веб-сервер.

---

## ⚠️ ВАЖНО: Перед открытием сайта

### 1. Создать базу данных:
Откройте в браузере:
```
http://localhost/rinks-moscow-app/backend/test/setup_database.php
```

### 2. Настроить config.php:
1. Скопируйте `backend/config/config.example.php` → `backend/config/config.php`
2. Проверьте настройки БД (обычно root, пустой пароль)

### 3. Настроить путь API:
Откройте `frontend/js/api.js` и проверьте:
```javascript
baseUrl: 'http://localhost/rinks-moscow-app/backend/api'
```
Измените, если путь другой!

---

## 🐛 Если не работает:

### Ошибка 404 (страница не найдена):
- Проверьте, что Apache запущен
- Проверьте путь к файлам
- Убедитесь, что проект в папке `htdocs`

### API не работает:
- Проверьте путь в `frontend/js/api.js`
- Проверьте, что `backend/config/config.php` создан
- Откройте консоль браузера (F12) и посмотрите ошибки

### База данных не работает:
- Проверьте, что MySQL запущен
- Запустите `setup_database.php`
- Проверьте настройки в `config.php`

---

## ✅ Проверка работы:

1. Откройте главную страницу
2. Должна загрузиться страница с поиском
3. Откройте консоль браузера (F12) → вкладка Console
4. Если есть ошибки - они будут видны там

---

## 📞 Быстрая проверка:

Откройте в браузере:
```
http://localhost/rinks-moscow-app/backend/api/rinks.php
```

Должен вернуться JSON:
```json
{
  "success": true,
  "data": {
    "results": [],
    "count": 0,
    "total": 0
  }
}
```

Если видите это - API работает! ✅
